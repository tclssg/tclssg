<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
        <title><% if {[info exists pageTitle]} { lindex "$pageTitle | $websiteTitle" } else { lindex $websiteTitle } %></title>
        <link rel="stylesheet" href="main.css">
    </head>
    <body>
        <%  set a {}
            if {[dict-default-get 0 $variables blogEntry]} {
                set a [concat $a {<div id="sidebar"><ul>}]
                foreach page $pageLinks {
                    set a [concat $a "<li><a href=\"$page\">$page</a></li>"]
                }
                set a [concat $a {</ul></div><!-- sidebar -->}]
            }
            lindex $a %>
        <div id="container">
        <% lindex $content %>
        </div><!-- #container -->
    </body>
</html>